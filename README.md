# ToDos:

1. Project picture of HashDB updating with real picture
2. On the About Me Page following things To do:
   1. all the Text Terminals as Commands in a New usable terminal on the page so that a command has to be used to show the terminal 
   2. adding Help text for it
   3. when one terminal text is already shown it should be taken away before another one is shown 